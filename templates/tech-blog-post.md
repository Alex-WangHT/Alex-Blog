---
# 【必填】发布日期，blog 插件按它排序和生成 URL（yyyy/MM/dd）
date:
  created: 2026-01-01
  # updated: 2026-01-02   # 修改后补上，文章页会显示"更新于"

# 【必填】标签，显示在文章页和 tech-blog/tags.md 标签索引页
tags:
  - 标签1
  - 标签2

# 【必填】分类，blog 插件用来生成分类页（建议沿用：机器人 / 人工智能 / 控制理论 / 计算机 / 电子设计 / 数学 / 物理）
categories:
  - 机器人

# 【可选】显示在首页"最新文章"列表里的一句话摘要
description: 一句话说明这篇文章讲什么

# 【可选】设为 true 则不会发布（本地 serve 时可见）
# draft: true
---

# 文章标题

> 导语：一两句话概括全文。这段会作为摘要显示在博客首页的文章卡片上。

<!-- more -->

## 第一节标题

正文开始……右侧栏的章节目录由 `##`、`###` 标题自动生成。

## 第二节标题

- 图片放同目录的 `img/` 文件夹，Obsidian 的 `![[xxx.png]]` 写法构建时会自动转换
- Obsidian 的 `> [!NOTE]` callout 也会自动转成网站的提示框
- 数学公式用 `$$ ... $$` 或行内 `$ ... $`
